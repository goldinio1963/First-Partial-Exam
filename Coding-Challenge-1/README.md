<h1>Commands</h1>
<p> 
    The commands use after the fork were
    -git clone
    -git status
    -git add .
    -git commit -m
    -git push
    then the pull but is in the github itself
</P>